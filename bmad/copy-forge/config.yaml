# Copy Forge Module Configuration
# Version: 2.0.0
# Results + Connection = Sustainable Conversion

module:
  code: copy-forge
  name: "Copy Forge"
  version: "2.0.0"
  description: "Landing page copy generator with sequential validation"

# Output
output_folder: "output/copy-forge"

# Data paths
data:
  brand_voice_folder: "bmad/copy-forge/data/brand-voice"
  avatars_folder: "bmad/copy-forge/data/avatars"

# Quality gates
quality_gates:
  voice_alignment: 100      # 7/7 criteria must pass
  audience_resonance: 100   # 8/8 criteria must pass
  word_count_tolerance: 10  # ±10%

# Pipeline
pipeline:
  feedback_loop:
    enabled: true
    max_rounds: 3
